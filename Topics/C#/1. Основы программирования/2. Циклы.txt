<b>Циклы в C#</b>

Циклы позволяют выполнять блок кода многократно, пока выполняется заданное условие. В C# существуют различные типы циклов, подходящие под разные задачи.

<b>🔁 Основные типы циклов:</b>
1. <b>Цикл for</b> — используется, когда известное количество итераций.
2. <b>Цикл while</b> — выполняется, пока условие истинно.
3. <b>Цикл do-while</b> — сначала выполняет тело цикла, затем проверяет условие.
4. <b>Цикл foreach</b> — предназначен для перебора элементов коллекции или массива.

<b>🗒️ Примеры кода:</b>

<b>1. Цикл for</b>
<pre><code>for (int i = 0; i &lt; 5; i++)
{
    Console.WriteLine(i);
}</code></pre>

<b>2. Цикл while</b>
<pre><code>int i = 0;
while (i &lt; 5)
{
    Console.WriteLine(i);
    i++;
}</code></pre>

<b>3. Цикл do-while</b>
<pre><code>int i = 0;
do
{
    Console.WriteLine(i);
    i++;
}
while (i &lt; 5);</code></pre>

<b>4. Цикл foreach</b>
<pre><code>string[] names = { "Alice", "Bob", "Charlie" };
foreach (string name in names)
{
    Console.WriteLine(name);
}</code></pre>

<b>✅ Итог:</b>
- <b>for</b> — удобен при известном числе итераций.
- <b>while</b> — проверяет условие до выполнения.
- <b>do-while</b> — выполняет тело хотя бы один раз.
- <b>foreach</b> — лучше всего подходит для перебора коллекций.
