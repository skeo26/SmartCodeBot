Question: Какая функция в MySQL позволяет выполнять условную проверку и возвращать одно значение, если условие истинно, и другое, если условие ложно?
Options:
- IF()
- CASE
- COALESCE()
- IFNULL()
Explanation: Функция IF() позволяет выполнять условную проверку и возвращать одно значение, если условие истинно, и другое, если условие ложно.
CorrectAnswerIndex: 0

Question: Какая функция в MySQL проверяет, является ли выражение NULL? Если это так, она возвращает заданное значение, иначе возвращает исходное выражение?
Options:
- IFNULL()
- CASE
- IF()
- COALESCE()
Explanation: Функция IFNULL() проверяет, является ли выражение NULL. Если это так, она возвращает заданное значение, иначе возвращает исходное выражение.
CorrectAnswerIndex: 0

Question: Какая функция в MySQL возвращает первое не-NULL значение среди переданных аргументов?
Options:
- COALESCE()
- IFNULL()
- CASE
- IF()
Explanation: Функция COALESCE() возвращает первое не-NULL значение среди переданных аргументов. Если все аргументы равны NULL, она возвращает NULL.
CorrectAnswerIndex: 0

Question: Какая конструкция в MySQL позволяет вернуть различные значения в зависимости от выполнения условий и проверяет несколько условий?
Options:
- CASE
- IF()
- IFNULL()
- COALESCE()
Explanation: Оператор CASE используется для выполнения условной логики в SQL-запросах и позволяет проверять несколько условий.
CorrectAnswerIndex: 0

Question: Какая функция в MySQL проверяет значение поля и, если оно равно NULL, возвращает заданное значение, иначе возвращает значение поля?
Options:
- IFNULL()
- COALESCE()
- IF()
- CASE
Explanation: Функция IFNULL() проверяет, является ли выражение NULL. Если это так, она возвращает заданное значение, иначе возвращает исходное выражение.
CorrectAnswerIndex: 0
