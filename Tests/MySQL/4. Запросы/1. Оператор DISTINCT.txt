Question: Для чего используется оператор DISTINCT в SQL?
Options:
- Для сортировки строк
- Для фильтрации строк по условию
- Для удаления дубликатов в результатах запроса
- Для объединения таблиц
Explanation: DISTINCT удаляет повторяющиеся строки из результата запроса.
CorrectAnswerIndex: 2

Question: Что вернёт запрос?
Code:
SELECT DISTINCT city FROM users;
Options:
- Все строки таблицы users
- Все города пользователей, включая повторы
- Только уникальные значения столбца city
- Уникальные пары name и city
Explanation: DISTINCT возвращает только уникальные значения указанного столбца.
CorrectAnswerIndex: 2

Question: Как работает запрос?
Code:
SELECT DISTINCT city, age FROM users;
Options:
- Возвращает уникальные города
- Возвращает уникальные значения age
- Возвращает уникальные комбинации city и age
- Возвращает все строки без изменений
Explanation: DISTINCT применяется ко всем указанным столбцам, поэтому возвращаются уникальные пары значений.
CorrectAnswerIndex: 2

Question: Можно ли использовать DISTINCT с несколькими столбцами?
Options:
- Нет, только с одним
- Да, но результат будет только по первому столбцу
- Да, результат будет уникален по комбинации всех указанных столбцов
- Да, но только в подзапросе
Explanation: DISTINCT применяется ко всем указанным столбцам сразу.
CorrectAnswerIndex: 2

Question: Какой недостаток может быть у DISTINCT?
Options:
- Он не работает с числовыми значениями
- Он изменяет данные в таблице
- Он требует дополнительной обработки и может быть медленным на больших объёмах
- Он всегда возвращает только одну строку
Explanation: DISTINCT требует дополнительных вычислений (например, сортировки), что может снизить производительность на больших таблицах.
CorrectAnswerIndex: 2
