Question: Что можно передавать в метод в качестве параметра?
Options:
- Только числа и строки
- Только логические значения
- Только структуры
- Объекты классов, включая пользовательские
Explanation: В C# методы могут принимать любые типы, включая пользовательские объекты.
CorrectAnswerIndex: 3

Question: Почему полезно передавать объект как параметр метода?
Options:
- Увеличивает размер кода
- Позволяет передавать сразу много связанных данных
- Делает метод приватным
- Позволяет использовать только статические члены
Explanation: Объекты могут содержать множество полей, которые удобно передавать вместе.
CorrectAnswerIndex: 1

Question: Что произойдёт при передаче объекта в метод?
Options:
- Создаётся копия объекта
- Передаётся ссылка на объект
- Метод не сможет использовать объект
- Создаётся новый объект
Explanation: В C# объекты (ссылочные типы) передаются по ссылке по умолчанию.
CorrectAnswerIndex: 1

Question: Какой результат выполнения метода PrintArea, если передан объект с Width = 4 и Height = 5?
Options:
- Площадь: 20
- Площадь: 9
- Площадь: 45
- Площадь: 0
Explanation: Метод вычисляет произведение ширины и высоты: 4 * 5 = 20.
CorrectAnswerIndex: 0

Question: Какой смысл у метода, возвращающего объект?
Options:
- Метод ничего не делает
- Метод возвращает ссылку на существующий объект
- Метод создаёт и возвращает новый объект
- Метод только выводит информацию
Explanation: Метод может создавать объект и возвращать его вызывающему коду.
CorrectAnswerIndex: 2

Question: Что делает ключевое слово ref при передаче объекта?
Options:
- Передаёт значение по копии
- Делает объект неизменяемым
- Передаёт ссылку на объект, позволяя заменить его
- Обновляет только поля объекта
Explanation: С помощью ref можно заменить объект внутри метода.
CorrectAnswerIndex: 2

Question: Какой модификатор параметра требует обязательного присваивания значения в методе?
Options:
- ref
- out
- static
- this
Explanation: out требует, чтобы переменной было присвоено значение внутри метода.
CorrectAnswerIndex: 1

Question: Что произойдёт, если изменить поля объекта, переданного в метод без ref или out?
Options:
- Ничего не изменится
- Изменения будут применены, так как объект передаётся по ссылке
- Объект скопируется и изменения не сохранятся
- Возникнет ошибка компиляции
Explanation: Даже без ref объект передаётся по ссылке — поля можно менять.
CorrectAnswerIndex: 1

Question: Какой из следующих вариантов вернёт объект из метода?
Options:
- return 5;
- return new Person();
- return "объект";
- return void;
Explanation: Метод может возвращать экземпляр объекта, например: return new Person();
CorrectAnswerIndex: 1

Question: Зачем использовать возврат объектов из метода?
Options:
- Чтобы сделать метод void
- Чтобы избежать использования классов
- Чтобы получить готовый объект с заполненными полями
- Чтобы уменьшить производительность
Explanation: Метод может создавать и настраивать объект, а затем возвращать его.
CorrectAnswerIndex: 2
